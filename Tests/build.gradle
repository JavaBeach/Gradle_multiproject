apply plugin: 'java'

repositories {
    jcenter()
}

dependencies {
    testCompile "junit:junit:4.12"
    compile project(':Functions')
    compile project(':Utils')
    compile project(':Starting')
}

